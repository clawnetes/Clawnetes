# Superpowers Install Plan

Date: 2026-04-08
Task: Fetch and follow instructions from `https://raw.githubusercontent.com/obra/superpowers/refs/heads/main/.codex/INSTALL.md`

## Plan

1. Fetch the remote install instructions and confirm the required steps.
2. Inspect the current local state for `~/.codex/superpowers`, `~/.agents/skills`, and any old bootstrap config.
3. Install or update the `superpowers` repository in `~/.codex/superpowers`.
4. Create or repair the `~/.agents/skills/superpowers` symlink.
5. Verify the symlink target and record any remaining manual step.

## Progress

- [x] Fetched and reviewed remote install instructions.
- [x] Inspected current local state.
- [x] Installed repository to `~/.codex/superpowers`.
- [x] Created `~/.agents/skills/superpowers` symlink.
- [x] Verified final state.

## Notes

- Current repo worktree already has an unrelated modification in `.gemini/settings.json`; this task will not touch it.
- Remote instructions require a Codex restart after install for skill discovery.
- `~/.codex/AGENTS.md` exists, but there was no old `superpowers-codex bootstrap` block to remove.
- Verified symlink target: `~/.agents/skills/superpowers -> ~/.codex/superpowers/skills`
