/**
 * This file is no longer needed — Playwright tests run against the Vite dev
 * server directly, so there is no tauri-driver process to manage.
 *
 * Kept as a placeholder in case a future migration to tauri-driver on
 * Linux/Windows CI is desired.
 */

export {};
